import { v4 as uuidv4 } from 'uuid';

export const io = null as any; // placeholder for TS to shut up, we will export it differently

// Dummy export to keep broadcaster ok. Real io will be assigned.
